/* Structural Modules */
import React from 'react';

export const StepTwo = () => (
  <div>
    <h1>Calling APIs</h1>
    <p>To call APIs from within your UI, configure a proxy (e.g. Apigee) service that contains CORS headers</p>
  </div>
);

export default StepTwo;